# 🔧 Исправление ограничения max_bet

## 🐛 Проблема

Кнопки быстрых ставок устанавливали неправильные значения из-за ограничения `max_bet`:

```
Fast bet clicked: 5000 Current: 2500 Active: undefined
Bet updated to: 500  ← ПРОБЛЕМА!
```

**Причина:**
В обработчике кнопок есть проверка:
```javascript
$val = $val < 0.5 ? 0.5 : ( $val > $game.max_bet ? $game.max_bet : $val );
```

Но `$game.max_bet` не обновлялся из API и оставался старым значением (например, 500), поэтому все ставки больше 500 обрезались до 500.

---

## ✅ Решение

Добавлено обновление `$game.max_bet` в функции `updateCountrySettings()`:

```javascript
// Update game instance max_bet (ВАЖНО!)
if (window.$game) {
    window.$game.max_bet = config.max_bet;
    console.log('✅ Game max_bet updated to:', config.max_bet);
}
```

---

## 🔄 Как это работает

### Шаг 1: API загружает конфигурацию страны
```javascript
const config = {
    quick_bets: [2500, 5000, 10000, 35000],
    min_bet: 100,
    max_bet: 70000,  // ← Максимальная ставка для Colombia
    default_bet: 2500
};
```

### Шаг 2: API обновляет max_bet в игре
```javascript
window.$game.max_bet = 70000;  // Для Colombia
```

### Шаг 3: Пользователь кликает на кнопку 5000
```javascript
var $val = 5000;  // Из data-bet-value
```

### Шаг 4: Проверка ограничений
```javascript
// Было: $game.max_bet = 500 (старое значение)
// $val = 5000 > 500 → $val = 500 ❌

// Стало: $game.max_bet = 70000 (обновленное)
// $val = 5000 < 70000 → $val = 5000 ✅
```

---

## 🧪 Тестирование

### Тест 1: Проверка max_bet в консоли

После загрузки страницы проверьте:
```javascript
console.log(window.$game.max_bet);
// Должно быть: 70000 (для Colombia)
```

### Тест 2: Клик на кнопку 5000

1. Кликните на кнопку **5000**
2. В консоли должно быть:
```
🔘 Fast bet clicked: {
  value: 5000,
  current: 2500,
  active: undefined,
  data-bet-value: "5000",
  text: "5000",
  max_bet: 70000  ← ВАЖНО!
}
🔘 Before limit check: 5000 max_bet: 70000
🔘 After limit check: 5000  ← Не обрезается!
✅ Bet updated to: 5000
```

### Тест 3: Клик на кнопку 35000

1. Кликните на кнопку **35000**
2. Поле ввода должно показывать: **35000**
3. В консоли:
```
🔘 Before limit check: 35000 max_bet: 70000
🔘 After limit check: 35000
✅ Bet updated to: 35000
```

### Тест 4: Попытка ввести больше max_bet

1. Введите вручную **100000** (больше max_bet 70000)
2. Значение должно обрезаться до **70000**

---

## 📊 Значения max_bet для разных стран

| Страна | Валюта | max_bet |
|--------|--------|---------|
| Colombia | COP | 70,000 |
| Paraguay | PYG | 1,500,000 |
| Ecuador | USD | 150 |
| Brazil | BRL | 1,000 |
| Argentina | ARS | 5,000 |
| Mexico | MXN | 3,000 |
| Peru | PEN | 1,000 |
| Chile | CLP | 200,000 |
| Uruguay | UYU | 10,000 |
| Bolivia | BOB | 2,000 |
| Venezuela | VES | 2,000,000 |
| Guyana | GYD | 50,000 |
| Suriname | SRD | 100,000 |

---

## 🔍 Отладка

### Проблема: Ставка обрезается

**Проверьте в консоли:**
```javascript
console.log('max_bet:', window.$game.max_bet);
console.log('Trying to set:', 5000);
console.log('Result:', 5000 > window.$game.max_bet ? window.$game.max_bet : 5000);
```

Если `max_bet` меньше желаемой ставки - значит он не обновился из API.

### Проблема: max_bet не обновляется

**Проверьте логи:**
```
✅ Game max_bet updated to: 70000
```

Если этого лога нет - значит `window.$game` не определен в момент вызова `updateCountrySettings()`.

---

## 📁 Измененные файлы

1. ✅ `res/js/api.js` - Добавлено обновление `$game.max_bet`
2. ✅ `res/js/game.js` - Добавлено подробное логирование

---

## ✅ Результат

Теперь кнопки быстрых ставок работают правильно:
- ✅ Кнопка **2500** → ставка **2500**
- ✅ Кнопка **5000** → ставка **5000** (не 500!)
- ✅ Кнопка **10000** → ставка **10000** (не 500!)
- ✅ Кнопка **35000** → ставка **35000** (не 500!)

**Ограничения работают правильно:**
- ✅ Минимальная ставка: **100 COP**
- ✅ Максимальная ставка: **70,000 COP**
- ✅ Значения между min и max не обрезаются
