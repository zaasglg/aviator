# 🧪 Руководство по тестированию API интеграции

## 📋 Что было реализовано

### 1. Логика работы со ставками
- ✅ Ставка вычитается при начале игры (не при размещении)
- ✅ При выигрыше добавляется выигрыш к балансу
- ✅ При проигрыше баланс не меняется
- ✅ Результат отправляется в API

### 2. API интеграция
- ✅ Создан файл `res/js/api.js` с классом `AviatorAPI`
- ✅ Поддержка всех необходимых endpoints
- ✅ Автоматическая инициализация из URL параметров
- ✅ Обработка ошибок и логирование

---

## 🔍 Как тестировать

### Тест 1: Демо режим (без API)

**URL:** `http://localhost/aviator/`

**Ожидаемое поведение:**
1. Игра загружается с демо балансом
2. При размещении ставки баланс НЕ меняется
3. При начале игры (полет самолета) баланс уменьшается на сумму ставки
4. При выигрыше баланс увеличивается на сумму выигрыша
5. При проигрыше баланс остается без изменений
6. API запросы НЕ отправляются

**Проверка в консоли:**
```javascript
console.log("Access token:", window.ACCESS_TOKEN); // Должно быть пусто
console.log("API instance:", window.$aviatorAPI.hasToken()); // false
```

---

### Тест 2: Реальный режим (с API)

**URL:** `http://localhost/aviator/?mode=real&access_token=YOUR_TOKEN`

**Ожидаемое поведение:**
1. Игра загружается с балансом из API
2. При размещении ставки баланс НЕ меняется
3. При начале игры баланс уменьшается на сумму ставки
4. При выигрыше:
   - Баланс увеличивается на сумму выигрыша
   - Отправляется PUT запрос в API с итоговым балансом
5. При проигрыше:
   - Баланс остается без изменений
   - Отправляется PUT запрос в API с итоговым балансом

**Проверка в консоли:**
```javascript
console.log("Access token:", window.ACCESS_TOKEN); // Должен быть токен
console.log("API instance:", window.$aviatorAPI.hasToken()); // true
console.log("User balance:", window.$user.balance); // Баланс из API
```

**Проверка Network (F12 → Network):**
- При загрузке: `GET /api/user/info/`
- После игры: `PUT /api/user/deposit/`

---

## 📊 Примеры тестовых сценариев

### Сценарий 1: Выигрыш

**Начальные данные:**
- Баланс: 1000 USD
- Ставка: 10 USD
- Коэффициент вывода: 2.5x

**Шаги:**
1. Размещаем ставку 10 USD
   - **Проверка:** Баланс = 1000 USD (не изменился)
2. Начинается игра (самолет взлетает)
   - **Проверка:** Баланс = 990 USD (вычтена ставка)
3. Нажимаем Cashout при коэффициенте 2.5x
   - **Проверка:** Баланс = 1015 USD (990 + 25)
   - **Проверка:** В консоли: "WIN: Balance after: 1015"
   - **Проверка:** API запрос: `{ "deposit": 1015.00 }`

---

### Сценарий 2: Проигрыш

**Начальные данные:**
- Баланс: 1000 USD
- Ставка: 10 USD

**Шаги:**
1. Размещаем ставку 10 USD
   - **Проверка:** Баланс = 1000 USD (не изменился)
2. Начинается игра (самолет взлетает)
   - **Проверка:** Баланс = 990 USD (вычтена ставка)
3. Самолет улетает (не успели вывести)
   - **Проверка:** Баланс = 990 USD (не изменился)
   - **Проверка:** В консоли: "LOSE: Balance remains: 990"
   - **Проверка:** API запрос: `{ "deposit": 990.00 }`

---

### Сценарий 3: Две ставки одновременно

**Начальные данные:**
- Баланс: 1000 USD
- Ставка 1: 10 USD
- Ставка 2: 20 USD

**Шаги:**
1. Размещаем обе ставки
   - **Проверка:** Баланс = 1000 USD
2. Начинается игра
   - **Проверка:** Баланс = 970 USD (1000 - 10 - 20)
3. Выводим первую ставку при 2x
   - **Проверка:** Баланс = 990 USD (970 + 20)
   - **Проверка:** API запрос: `{ "deposit": 990.00 }`
4. Самолет улетает (вторая ставка проиграна)
   - **Проверка:** Баланс = 990 USD (не изменился)
   - **Проверка:** API запрос: `{ "deposit": 990.00 }`

---

## 🐛 Отладка

### Включить подробное логирование

Откройте консоль браузера (F12) и проверьте логи:

**При начале игры:**
```
=== НАЧАЛО ИГРЫ (Ставка) ===
Balance before bets: 1000
Deducting bet for button 1 : 10
Balance after bet 1 : 990
Final balance after all bets: 990
```

**При выигрыше:**
```
=== WIN ===
WIN: Bet amount: 10
WIN: Coefficient: 2.5
WIN: Win amount: 25
WIN: Balance before: 990
WIN: Balance after: 1015
WIN: Sending result to API
```

**При проигрыше:**
```
=== КОНЕЦ ИГРЫ ===
=== LOSE ===
LOSE: Bet amount: 10
LOSE: Balance remains: 990 (bet was already deducted at start)
LOSE: Sending result to API
```

---

## ✅ Чек-лист тестирования

### Демо режим
- [ ] Игра загружается
- [ ] Баланс отображается корректно
- [ ] Ставка размещается
- [ ] При начале игры баланс уменьшается
- [ ] При выигрыше баланс увеличивается
- [ ] При проигрыше баланс не меняется
- [ ] API запросы НЕ отправляются

### Реальный режим
- [ ] Игра загружается с токеном
- [ ] Баланс загружается из API
- [ ] Валюта обновляется из API
- [ ] Ставка размещается
- [ ] При начале игры баланс уменьшается
- [ ] При выигрыше:
  - [ ] Баланс увеличивается
  - [ ] API запрос отправляется
  - [ ] Баланс синхронизируется с сервером
- [ ] При проигрыше:
  - [ ] Баланс не меняется
  - [ ] API запрос отправляется
  - [ ] Баланс синхронизируется с сервером

### API интеграция
- [ ] GET /api/user/info/ работает
- [ ] PUT /api/user/deposit/ работает
- [ ] Токен передается в заголовках
- [ ] Ошибки обрабатываются корректно
- [ ] Баланс округляется до 2 знаков

---

## 🔧 Устранение неполадок

### Проблема: API запросы не отправляются

**Решение:**
1. Проверьте наличие токена: `console.log(window.ACCESS_TOKEN)`
2. Проверьте режим: `console.log(window.$aviatorAPI.isRealMode)`
3. Проверьте URL: должен содержать `?access_token=...`

### Проблема: Баланс не обновляется

**Решение:**
1. Откройте консоль и проверьте ошибки
2. Проверьте Network (F12 → Network) на наличие запросов
3. Проверьте ответ API: должен содержать `balance` или `new_deposit`

### Проблема: CORS ошибка

**Решение:**
1. Убедитесь, что API сервер настроен для CORS
2. Проверьте заголовки в Network
3. Проверьте, что токен валидный

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера на ошибки
2. Проверьте Network на API запросы
3. Проверьте логи в консоли (см. раздел "Отладка")
4. Убедитесь, что все файлы обновлены:
   - `res/js/api.js`
   - `res/js/game.js`
   - `templates/main.tpl.php`
