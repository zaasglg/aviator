# 🔘 Исправление кнопок быстрых ставок

## 🐛 Проблема

Кнопки быстрых ставок отображали правильные значения (2500, 5000, 10000, 35000), но:
- ❌ Не все кнопки кликались
- ❌ При клике на 5000 ставка становилась 500
- ❌ Некоторые кнопки вообще не работали

**Причина:**
Обработчики событий (click handlers) использовали старые значения из текста кнопок, а не обновленные значения.

---

## ✅ Решение

### 1. Добавлен data-атрибут для хранения значения

**В `res/js/api.js`:**
```javascript
// Сохраняем значение в data-атрибуте
$btn.attr('data-bet-value', value);
```

### 2. Обновлен обработчик в game.js

**Было:**
```javascript
var $val = parseFloat( $self.text() ); // Брали из текста
```

**Стало:**
```javascript
// Используем data-bet-value если есть, иначе текст кнопки
var $val = $self.attr('data-bet-value') ? 
    parseFloat($self.attr('data-bet-value')) : 
    parseFloat($self.text());
```

### 3. Убрана конфликтующая логика

API больше не переназначает обработчики событий - только обновляет:
- ✅ Текст кнопки (для отображения)
- ✅ Data-атрибут (для обработчика)

Обработчики остаются те, что установлены в game.js

---

## 🔄 Как это работает

### Шаг 1: API загружает конфигурацию страны
```javascript
const config = {
    quick_bets: [2500, 5000, 10000, 35000]
};
```

### Шаг 2: API обновляет кнопки
```javascript
$btn.text('2500');                    // Визуальное отображение
$btn.attr('data-bet-value', 2500);    // Реальное значение
```

### Шаг 3: Пользователь кликает на кнопку
```javascript
// Обработчик в game.js читает data-bet-value
var $val = parseFloat($self.attr('data-bet-value')); // 2500
```

### Шаг 4: Ставка обновляется
```javascript
$('input:text', $wrap).val(2500);
$('[data-rel="current_bet"]', $wrap).html(2500);
```

---

## 🧪 Тестирование

### Тест 1: Проверка кнопок

1. Откройте игру с токеном
2. Проверьте консоль - должны быть логи:
```
🔘 Updating 4 quick bet buttons
🔘 Button 0 set to: 2500 display: 2500
🔘 Button 1 set to: 5000 display: 5000
🔘 Button 2 set to: 10000 display: 10000
🔘 Button 3 set to: 35000 display: 35000
```

### Тест 2: Клик на кнопку 2500

1. Кликните на кнопку **2500**
2. В консоли должно быть:
```
Fast bet clicked: 2500 Current: 2500 Active: undefined
Bet updated to: 2500
```
3. Поле ввода должно показывать: **2500**

### Тест 3: Клик на кнопку 5000

1. Кликните на кнопку **5000**
2. В консоли должно быть:
```
Fast bet clicked: 5000 Current: 2500 Active: undefined
Bet updated to: 5000
```
3. Поле ввода должно показывать: **5000** (не 500!)

### Тест 4: Клик на кнопку 10000

1. Кликните на кнопку **10000**
2. Поле ввода должно показывать: **10000**

### Тест 5: Клик на кнопку 35000

1. Кликните на кнопку **35000**
2. Поле ввода должно показывать: **35000**

---

## 📊 Структура данных

### HTML кнопки после обновления:
```html
<button class="fast_bet" data-bet-value="2500">2500</button>
<button class="fast_bet" data-bet-value="5000">5000</button>
<button class="fast_bet" data-bet-value="10000">10000</button>
<button class="fast_bet" data-bet-value="35000">35000</button>
```

### Обработчик в game.js:
```javascript
$('#actions_wrapper .actions_field .fast_bet').on('click', function(){
    var $self = $(this);
    var $val = $self.attr('data-bet-value') ? 
        parseFloat($self.attr('data-bet-value')) : 
        parseFloat($self.text());
    
    // Обновляем ставку
    $('input:text', $wrap).val($val);
    $('[data-rel="current_bet"]', $wrap).html($val);
});
```

---

## 🔍 Отладка

### Проблема: Кнопка не кликается

**Проверьте:**
1. Есть ли `data-bet-value` у кнопки?
```javascript
console.log($('.fast_bet').first().attr('data-bet-value'));
```

2. Установлен ли обработчик?
```javascript
console.log($._data($('.fast_bet')[0], 'events'));
```

### Проблема: Неправильное значение

**Проверьте консоль:**
```
Fast bet clicked: 5000 Current: 2500 Active: undefined
```

Если показывает `500` вместо `5000` - значит data-атрибут не установлен.

---

## 📁 Измененные файлы

1. ✅ `res/js/api.js` - Обновление кнопок с data-атрибутами
2. ✅ `res/js/game.js` - Чтение значения из data-атрибута

---

## ✅ Результат

Теперь все кнопки быстрых ставок работают правильно:
- ✅ Кликаются все 4 кнопки
- ✅ Устанавливают правильные значения
- ✅ Значения соответствуют стране пользователя
- ✅ Логирование в консоли для отладки

**Пример для Colombia:**
- Кнопка **2500** → ставка **2500** ✅
- Кнопка **5000** → ставка **5000** ✅ (не 500!)
- Кнопка **10000** → ставка **10000** ✅
- Кнопка **35000** → ставка **35000** ✅
