# 👤 Маскировка имен пользователей

## 🎯 Задача

Сделать имена пользователей не полными, а с маскировкой для конфиденциальности.

**Было:**
```
Alex
Maria
John
Demo Player
```

**Стало:**
```
a****x
m****a
j****n
d****r
```

---

## ✅ Решение

### 1. Добавлена функция маскировки имен

```javascript
// Функция для маскировки имени (например, "Alex" -> "a****x")
maskName(name) {
    if (!name || name.length < 2) return name;
    
    var firstChar = name.charAt(0).toLowerCase();
    var lastChar = name.charAt(name.length - 1);
    var stars = '****';
    
    return firstChar + stars + lastChar;
}
```

### 2. Обновлена функция generateFakeUsers

**Было:**
```javascript
users.push({
    uid: 1000 + i,
    name: fakeNames[i],  // Полное имя
    img: Math.floor(Math.random() * 20) + 1
});
```

**Стало:**
```javascript
var fullName = fakeNames[i];
var maskedName = this.maskName(fullName);

users.push({
    uid: 1000 + i,
    name: maskedName,      // Маскированное имя
    fullName: fullName,    // Полное имя (на случай если понадобится)
    img: Math.floor(Math.random() * 20) + 1
});
```

### 3. Маскировка имени реального пользователя

```javascript
var userName = $user.real_name || $user.name || "You";
var maskedName = $game.maskName(userName);

var userBet = {
    uid: $user.uid,
    name: maskedName,  // Используем маскированное имя
    amount: parseFloat($data.bet),
    // ...
};
```

---

## 📊 Примеры маскировки

| Полное имя | Маскированное |
|------------|---------------|
| Alex | a****x |
| Maria | m****a |
| John | j****n |
| Anna | a****a |
| Mike | m****e |
| Lisa | l****a |
| David | d****d |
| Sarah | s****h |
| Tom | t****m |
| Emma | e****a |
| Demo Player | d****r |
| Player | p****r |
| You | y****u |

---

## 🔄 Как это работает

### Шаг 1: Генерация фейковых пользователей

```javascript
var fakeNames = ["Alex", "Maria", "John", ...];

for (var i = 0; i < fakeNames.length; i++) {
    var fullName = fakeNames[i];        // "Alex"
    var maskedName = this.maskName(fullName);  // "a****x"
    
    users.push({
        name: maskedName  // Сохраняем маскированное имя
    });
}
```

### Шаг 2: Отображение в списке ставок

```html
<li data-uid="1000">
    <div class="user">
        <img src="res/img/users/av-1.png" alt="">
        <span>a****x</span>  <!-- Маскированное имя -->
    </div>
    <div class="bet">10.00</div>
    <div class="betx">2.5x</div>
    <div class="win">25.00</div>
</li>
```

---

## 🧪 Тестирование

### Тест 1: Проверка фейковых пользователей

1. Откройте игру
2. Дождитесь начала раунда
3. Проверьте список ставок слева
4. Все имена должны быть в формате `x****y`

### Тест 2: Проверка реального пользователя

1. Сделайте ставку
2. Ваше имя в списке должно быть маскировано
3. Например: "Demo Player" → "d****r"

### Тест 3: Проверка в консоли

```javascript
console.log($game.maskName("Alex"));      // "a****x"
console.log($game.maskName("Maria"));     // "m****a"
console.log($game.maskName("Demo Player")); // "d****r"
```

---

## 🎨 Визуальный результат

### До:
```
👤 Alex      10.00  2.5x  25.00
👤 Maria      5.00  1.8x   9.00
👤 John      20.00  3.2x  64.00
👤 Demo Player 15.00  2.1x  31.50
```

### После:
```
👤 a****x    10.00  2.5x  25.00
👤 m****a     5.00  1.8x   9.00
👤 j****n    20.00  3.2x  64.00
👤 d****r    15.00  2.1x  31.50
```

---

## 🔒 Конфиденциальность

### Преимущества маскировки:

1. ✅ **Конфиденциальность** - полные имена не видны другим игрокам
2. ✅ **Безопасность** - сложнее идентифицировать конкретного пользователя
3. ✅ **Профессионализм** - соответствует стандартам онлайн-казино
4. ✅ **Единообразие** - все имена в одном формате

### Что маскируется:

- ✅ Имена фейковых пользователей (Alex → a****x)
- ✅ Имя реального пользователя (Demo Player → d****r)
- ✅ Имена в списке текущих ставок
- ✅ Имена в списке предыдущих ставок

---

## 📁 Измененные файлы

1. ✅ `res/js/game.js` - Добавлена функция `maskName()` и обновлены функции генерации пользователей

---

## ✅ Результат

Теперь все имена пользователей отображаются в маскированном виде:
- ✅ Первая буква (строчная)
- ✅ Четыре звездочки (****)
- ✅ Последняя буква

**Формат:** `x****y`

**Примеры:**
- Alex → a****x
- Maria → m****a
- Demo Player → d****r
- You → y****u
