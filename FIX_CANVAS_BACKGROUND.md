# 🎨 Восстановление фона canvas

## 🐛 Проблема

Фон игрового поля (canvas) был пустым или слишком темным, не было визуальной глубины.

---

## ✅ Решение

Добавлен градиентный фон с сеткой для создания эффекта глубины.

### 1. Градиентный фон

```javascript
// Рисуем градиентный фон
var gradient = $ctx.createLinearGradient(0, 0, 0, SETTINGS.h);
gradient.addColorStop(0, "#001a33");    // Темно-синий сверху
gradient.addColorStop(0.5, "#002244");  // Средний синий
gradient.addColorStop(1, "#001122");    // Темно-синий снизу

$ctx.fillStyle = gradient;
$ctx.fillRect(0, 0, SETTINGS.w, SETTINGS.h);
```

### 2. Сетка для эффекта глубины

```javascript
// Добавляем сетку для эффекта глубины
$ctx.strokeStyle = "rgba(0, 100, 150, 0.1)";
$ctx.lineWidth = 1;

// Вертикальные линии
for (var x = 0; x < SETTINGS.w; x += 50) {
    $ctx.beginPath();
    $ctx.moveTo(x, 0);
    $ctx.lineTo(x, SETTINGS.h);
    $ctx.stroke();
}

// Горизонтальные линии
for (var y = 0; y < SETTINGS.h; y += 50) {
    $ctx.beginPath();
    $ctx.moveTo(0, y);
    $ctx.lineTo(SETTINGS.w, y);
    $ctx.stroke();
}
```

---

## 🎨 Визуальный результат

### Градиент:
```
┌─────────────────────┐
│   #001a33 (верх)    │  ← Темно-синий
│                     │
│   #002244 (центр)   │  ← Средний синий
│                     │
│   #001122 (низ)     │  ← Темно-синий
└─────────────────────┘
```

### Сетка:
```
┌─┬─┬─┬─┬─┬─┬─┬─┬─┬─┐
├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
└─┴─┴─┴─┴─┴─┴─┴─┴─┴─┘
```

---

## 🔄 Как это работает

### Каждый кадр (60 FPS):

1. **Очистка canvas**
```javascript
$ctx.clearRect(0, 0, SETTINGS.w, SETTINGS.h);
```

2. **Отрисовка градиента**
```javascript
$ctx.fillStyle = gradient;
$ctx.fillRect(0, 0, SETTINGS.w, SETTINGS.h);
```

3. **Отрисовка сетки**
```javascript
// Вертикальные и горизонтальные линии
```

4. **Отрисовка игровых элементов**
```javascript
$game.update({});
$plane.update({});
```

---

## 🎯 Преимущества

### Градиент:
- ✅ **Глубина** - создает ощущение пространства
- ✅ **Профессионализм** - выглядит как настоящая игра
- ✅ **Контраст** - самолет и график хорошо видны

### Сетка:
- ✅ **Ориентация** - помогает отслеживать движение
- ✅ **Эффект глубины** - создает 3D ощущение
- ✅ **Не отвлекает** - полупрозрачная (opacity: 0.1)

---

## 🎨 Цветовая схема

| Позиция | Цвет | RGB | Описание |
|---------|------|-----|----------|
| Верх (0%) | #001a33 | 0, 26, 51 | Темно-синий |
| Центр (50%) | #002244 | 0, 34, 68 | Средний синий |
| Низ (100%) | #001122 | 0, 17, 34 | Темно-синий |
| Сетка | rgba(0, 100, 150, 0.1) | 0, 100, 150, 10% | Полупрозрачный синий |

---

## ⚙️ Настройка

### Изменить цвета градиента:

```javascript
gradient.addColorStop(0, "#YOUR_COLOR_TOP");
gradient.addColorStop(0.5, "#YOUR_COLOR_MIDDLE");
gradient.addColorStop(1, "#YOUR_COLOR_BOTTOM");
```

### Изменить плотность сетки:

```javascript
// Более плотная сетка (каждые 25px)
for (var x = 0; x < SETTINGS.w; x += 25) { ... }

// Менее плотная сетка (каждые 100px)
for (var x = 0; x < SETTINGS.w; x += 100) { ... }
```

### Изменить прозрачность сетки:

```javascript
// Более видимая сетка
$ctx.strokeStyle = "rgba(0, 100, 150, 0.3)";

// Менее видимая сетка
$ctx.strokeStyle = "rgba(0, 100, 150, 0.05)";
```

### Убрать сетку (только градиент):

Просто закомментируйте код сетки:
```javascript
// Добавляем сетку для эффекта глубины (опционально)
// $ctx.strokeStyle = "rgba(0, 100, 150, 0.1)";
// ...
```

---

## 🧪 Тестирование

### Тест 1: Проверка градиента

1. Откройте игру
2. Посмотрите на фон
3. Должен быть плавный переход от темно-синего к среднему синему

### Тест 2: Проверка сетки

1. Посмотрите внимательно на фон
2. Должны быть видны тонкие линии сетки
3. Сетка не должна отвлекать от игры

### Тест 3: Проверка производительности

1. Откройте консоль (F12)
2. Перейдите на вкладку Performance
3. FPS должен быть стабильным (60 FPS)

---

## 📁 Измененные файлы

1. ✅ `res/js/game.js` - Обновлена функция `render()` с градиентом и сеткой

---

## ✅ Результат

Теперь игровое поле имеет:
- ✅ Красивый градиентный фон
- ✅ Сетку для эффекта глубины
- ✅ Профессиональный вид
- ✅ Хорошую видимость игровых элементов

**Фон всегда отображается** и создает атмосферу игры!
