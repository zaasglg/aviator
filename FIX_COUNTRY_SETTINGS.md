# 🌍 Исправление настроек ставок для разных стран

## 🐛 Проблема

В реальном режиме отображались неправильные значения быстрых ставок (quick_bets).

**Пример:**
- Пользователь из **Colombia** (валюта COP)
- Должны быть ставки: **2500, 5000, 10000, 35000**
- Отображались: **50000, 100000, 200000, 700000** (настройки для Paraguay)

---

## ✅ Решение

Добавлена функция `updateCountrySettings()` в `res/js/api.js`, которая:
1. Получает страну пользователя из API
2. Загружает соответствующую конфигурацию
3. Обновляет кнопки быстрых ставок
4. Обновляет min/max bet и default bet

---

## 📊 Конфигурации стран

Все конфигурации синхронизированы с `demo_config.php`:

### Colombia (COP)
```javascript
quick_bets: [2500, 5000, 10000, 35000]
min_bet: 100
max_bet: 70000
default_bet: 2500
```

### Paraguay (PYG)
```javascript
quick_bets: [50000, 100000, 200000, 700000]
min_bet: 1000
max_bet: 1500000
default_bet: 50000
```

### Ecuador (USD)
```javascript
quick_bets: [0.5, 1, 2, 7]
min_bet: 0.5
max_bet: 150
default_bet: 0.5
```

### Brazil (BRL)
```javascript
quick_bets: [20, 50, 100, 350]
min_bet: 10
max_bet: 1000
default_bet: 20
```

### Argentina (ARS)
```javascript
quick_bets: [150, 300, 600, 2100]
min_bet: 50
max_bet: 5000
default_bet: 150
```

### Mexico (MXN)
```javascript
quick_bets: [100, 200, 400, 1400]
min_bet: 50
max_bet: 3000
default_bet: 100
```

### Peru (PEN)
```javascript
quick_bets: [20, 50, 100, 350]
min_bet: 10
max_bet: 1000
default_bet: 20
```

### Chile (CLP)
```javascript
quick_bets: [5000, 10000, 20000, 70000]
min_bet: 1000
max_bet: 200000
default_bet: 5000
```

### Uruguay (UYU)
```javascript
quick_bets: [200, 400, 800, 2800]
min_bet: 100
max_bet: 10000
default_bet: 200
```

### Bolivia (BOB)
```javascript
quick_bets: [35, 70, 140, 490]
min_bet: 10
max_bet: 2000
default_bet: 35
```

### Venezuela (VES)
```javascript
quick_bets: [50000, 100000, 200000, 700000]
min_bet: 10000
max_bet: 2000000
default_bet: 50000
```

### Guyana (GYD)
```javascript
quick_bets: [1000, 2000, 4000, 14000]
min_bet: 500
max_bet: 50000
default_bet: 1000
```

### Suriname (SRD)
```javascript
quick_bets: [2000, 4000, 8000, 28000]
min_bet: 1000
max_bet: 100000
default_bet: 2000
```

---

## 🔄 Как это работает

### 1. API возвращает информацию о пользователе
```json
{
  "user_id": 311,
  "deposit": 309969999,
  "country": "Colombia",
  "country_info": {
    "currency": "COP",
    "name": "Colombia"
  }
}
```

### 2. Функция `updateCountrySettings()` вызывается автоматически
```javascript
if (data.country) {
    this.updateCountrySettings(data.country, data.country_info.currency);
}
```

### 3. Обновляются настройки
- ✅ Кнопки быстрых ставок
- ✅ Значение по умолчанию в поле ввода
- ✅ Min/Max bet в `window.$user`
- ✅ Конфигурация в `window.$game_config`

---

## 🧪 Тестирование

### Шаг 1: Откройте игру с токеном
```
https://valor-games.com/aviator/?access_token=YOUR_TOKEN
```

### Шаг 2: Проверьте консоль
Вы должны увидеть:
```
🌍 Updating country settings for: Colombia COP
🌍 Country config: {currency: "COP", quick_bets: [2500, 5000, 10000, 35000], ...}
✅ User settings updated: {quick_bets: [2500, 5000, 10000, 35000], ...}
✅ Country settings updated for Colombia
```

### Шаг 3: Проверьте кнопки ставок
Для Colombia должны отображаться:
- **2500**
- **5000**
- **10000**
- **35000**

### Шаг 4: Проверьте поле ввода
Значение по умолчанию должно быть **2500** (для Colombia)

---

## 📁 Измененные файлы

- ✅ `res/js/api.js` - Добавлена функция `updateCountrySettings()`

---

## ✅ Результат

Теперь в реальном режиме настройки ставок соответствуют стране пользователя, как в демо режиме!

**Пример для Colombia:**
- Валюта: **COP**
- Быстрые ставки: **2500, 5000, 10000, 35000**
- Ставка по умолчанию: **2500**
- Min bet: **100**
- Max bet: **70000**
